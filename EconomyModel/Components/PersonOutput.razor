@using EconomyModellingEngine;
<h5>Person</h5>


<p>Necessary stockpile: @PersonOutputPerTick.NecessaryStockPileAtEnd</p>

<p>Consumption:</p>
<NecessaryAndLuxury NecessaryAndLuxuryField="@PersonOutputPerTick.Consumption" />

@code {
    [EditorRequired]
    [Parameter]
    public PersonOutputPerTick PersonOutputPerTick { get; set; } = null!;
}
